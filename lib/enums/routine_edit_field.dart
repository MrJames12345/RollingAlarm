/// Edit form fields that summary tiles can deep link into.
enum RoutineEditFieldEnum {
  sound,
  volume,
  fadeIn,
  vibrate,
  interval,
  snooze,
  maxTimesPerDay,
  maxTimesLimit,
  dayStart,
  driftCompensation,
  days,
}
